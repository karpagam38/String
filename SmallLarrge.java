import java.util.Scanner;

public class SmallLarrge {
	String s=new String();
	int count,c1,c2;
	Scanner scan=new Scanner(System.in);
	void getinput()
	{
		System.out.println("Enther the sentence");
		s=scan.nextLine();
	}
	void process()
	{
		String s s=s.split; 
		
		/*
		 * char ch=s.charAt(0); for(int i=0;i<s.length();i++) {
		 * if(s.charAt(i)>='a'&&s.charAt(i)<='z') {count++;}
		 * 
		 * else if(count<=0) {ch++;}
		 * 
		 * else if (count>0 ) {ch++;} }
		 */
			/*
			 * if(ch>0) { System.out.println("large number: "+ch); } else {
			 * System.out.println("Smallest number: "+ch); }
			 */
		
			{
			}
			System.out.println("Smallest Word: "+ch);
			System.out.println("Largest Word: "+ch);
	}
}

